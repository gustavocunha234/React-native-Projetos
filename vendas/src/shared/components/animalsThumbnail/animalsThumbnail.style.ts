import styled from "styled-components/native";
import { theme } from "../../themes/themes";

export const AnimalsThumbnailContainer = styled.TouchableOpacity`
    height: 120px;
    border-radius: 4px;
    width: 40px;
    border: 1px solid ${theme.colors.grayTheme}
`